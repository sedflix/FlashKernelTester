#include <stdio.h>
int main()
{  
    printf("HELLO WORLD!!! IT WORKS!! \n \n");

    printf("You remember the init process? It is the first process to be executed. \n \
Before init starts to execute, the kernel is executing. \n \
The test.c file is changed to a static binary and is passed to the kernel as the init process. \n \
The kernel executes the the static binary and it exits. IDK why \n");

    printf("\n \n \t THE FLASH!!! \n");
    return 0;
}
